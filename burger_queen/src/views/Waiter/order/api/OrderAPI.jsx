import React from "react";
import { useState, useEffect } from "react";

const OrderAPI = (customer, name, quantity, price, total, item) => {
  {[
    id: `generado automaticamente`
    customer: Eugenia, {
      product: item.id,
      (name: item.name, )
      quantity: x2, 
      (price: item.price, )
    },
    {
      name: americano, 
      quantity: x1, 
      price: $5, 
    }
    total: $19
    
  ]}

  return <div>OrderAPI</div>;
};

export default OrderAPI;
